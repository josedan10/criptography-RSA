int func_texto2num(char *texto, char *esquema, char *numeros){
	int largo;
	if (strlen(texto)%2!=0)

	for (i=0; i<strlen(*esquema); i++){
		if(strcmp(esquema[i],letra)==0){
			break;
		}
	}
	return i;
}